# Additional dependencies for the collector-playwright service.
# These are NOT installed in the base collector image to keep it small.
#
# Browser engine priority (BrowserPool._start tries in order):
#   1. camoufox  — antidetect Firefox; bypasses Cloudflare JS challenge (~99%)
#                  and lowers Akamai/Kasada fingerprint score.
#                  Requires: pip install camoufox[geoip] && python -m camoufox fetch
#   2. patchright — patched Chromium (patches Runtime.enable CDP leak).
#                  Cannot be installed alongside vanilla playwright.
#   3. playwright — vanilla Chromium (last resort, already in base image).
camoufox[geoip]>=0.4.0
patchright>=1.50.0,<2.0
