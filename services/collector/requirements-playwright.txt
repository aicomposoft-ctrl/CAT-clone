# Additional dependencies for the collector-playwright service.
# These are NOT installed in the base collector image to keep it small.
#
# patchright is a drop-in replacement for playwright that patches the
# Runtime.enable CDP leak detected by Cloudflare/Akamai. It cannot be
# installed alongside playwright (they provide the same module API).
# The base image (mcr.microsoft.com/playwright/python:v1.44.0-jammy) has
# playwright pre-installed; pip install patchright upgrades/replaces it.
# BrowserPool._start() tries patchright first, falls back to playwright.
patchright>=1.50.0,<2.0
